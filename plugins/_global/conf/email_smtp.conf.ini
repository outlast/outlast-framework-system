# API setup (faster, default)
email_use_api = true
email_provider = postmark
email_api_key = 

# SMTP setup (used if API is set to false)
email_smtp_server = smtp.postmarkapp.com
email_smtp_port = 25
email_smtp_user =
email_smtp_pass =